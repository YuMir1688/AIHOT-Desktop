# 安全说明

发现凭据泄露或可利用漏洞时，不要在公开 Issue 中粘贴敏感内容。请使用 GitHub Security 页的私密漏洞报告入口（若尚未启用，请先联系仓库维护者安排私密沟通）。

所有设置、缓存、历史记录和 Telegram 配置应保留在本机 AppData。不要将这些目录上传仓库。若 token 已暴露，应立即在服务提供方撤销并更换，删除 Git 提交本身不能撤回已泄露的凭据。

本程序会访问资讯服务；打开原文会调用默认浏览器。Telegram 为可选功能，未配置时不应发送消息。
